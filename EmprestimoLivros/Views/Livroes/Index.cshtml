@model IEnumerable<EmprestimoLivros.Models.Livro>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Livros</h1>

<p>
    <a asp-action="Create">Novo</a>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.anoPublicacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.emprestado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Autor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.anoPublicacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.emprestado)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.LivroId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LivroId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LivroId">Delete</a>
*@
                    <div class="btn-group" role="button">
                            <a role="button" class="btn btn-primary" asp-controller="Livroes" asp-route-id="@item.LivroId" asp-action="Edit">Editar</a>
                        <a role="button" class="btn btn-danger" asp-controller="Livroes" asp-route-id="@item.LivroId" asp-action="Delete">Apagar</a>

                    </div>
            </td>
        </tr>
}
    </tbody>
</table>
